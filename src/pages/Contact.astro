---
// Halaman Contact - Indonesia Travel
import Footer from '../components/Footer.astro';
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact - Indonesia Travel</title>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&family=Noto+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Noto Sans', sans-serif;
            opacity: 0;
            transition: opacity 0.5s cubic-bezier(0.4,0,0.2,1);
            background: #f7f7f7;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        
        body.fade-out {
            opacity: 0;
            transition: opacity 0.5s cubic-bezier(0.4,0,0.2,1);
        }

        /* Header Styles */
        .header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 20px 0;
            background: transparent;
            transition: all 0.3s ease;
            font-family: 'Noto Sans', sans-serif;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 40px;
        }

        .logo {
            color: #333;
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.8rem;
            font-weight: 700;
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .logo-highlight {
            color: #ff6b6b;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 40px;
            font-family: 'Noto Sans', sans-serif;
        }

        .nav-menu a {
            color: #333;
            text-decoration: none;
            font-weight: 500;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            transition: all 0.3s ease;
        }

        .nav-menu a:hover {
            color: #ff6b6b;
        }

        .nav-menu a:after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 50%;
            background-color: #ff6b6b;
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }

        .nav-menu a:hover:after {
            width: 100%;
        }

        .nav-menu a.active {
            color: #ff6b6b;
        }

        .nav-menu a.active:after {
            width: 100%;
        }

        .hamburger {
            display: none;
            flex-direction: column;
            cursor: pointer;
            gap: 4px;
        }

        .hamburger span {
            width: 25px;
            height: 3px;
            background-color: #fff;
            transition: all 0.3s ease;
            border-radius: 2px;
        }

        .header.scrolled .hamburger span {
            background-color: #333;
        }

        .hamburger.active span:nth-child(1) {
            transform: rotate(45deg) translate(5px, 5px);
        }

        .hamburger.active span:nth-child(2) {
            opacity: 0;
        }

        .hamburger.active span:nth-child(3) {
            transform: rotate(-45deg) translate(7px, -6px);
        }

        /* Mobile Sidebar */
        .mobile-sidebar {
            position: fixed;
            top: 0;
            right: -300px;
            width: 300px;
            height: 100vh;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            z-index: 9999;
            transition: right 0.3s ease;
            padding: 80px 30px 30px;
            box-shadow: -5px 0 20px rgba(0, 0, 0, 0.1);
        }

        .mobile-sidebar.active {
            right: 0;
        }

        .mobile-sidebar .nav-menu {
            flex-direction: column;
            gap: 20px;
            display: flex;
        }

        .mobile-sidebar .nav-menu a {
            color: #2c3e50;
            font-size: 16px;
            font-weight: 500;
            padding: 15px 0;
            border-bottom: 1px solid rgba(0, 0, 0, 0.08);
            text-shadow: none;
            transition: all 0.3s ease;
        }

        .mobile-sidebar .nav-menu a:hover {
            color: #ff6b6b;
            transform: translateX(5px);
        }

        /* Overlay */
        .mobile-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 9998;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .mobile-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        @media (max-width: 768px) {
            .nav-menu {
                display: none;
            }

            .hamburger {
                display: flex;
            }

            .nav-container {
                padding: 0 20px;
            }

            .header {
                padding: 15px 0;
            }

            .logo {
                font-size: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .nav-container {
                padding: 0 15px;
            }

            .logo {
                font-size: 1.4rem;
            }

            .header {
                padding: 12px 0;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <nav class="nav-container">
            <a href="/" class="logo">
                INDO<span class="logo-highlight">NESIA</span>
            </a>
            <ul class="nav-menu">
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/product">Product</a></li>
                <li><a href="/Gallery">Gallery</a></li>
                <li><a href="/Contact" class="active">Contact</a></li>
            </ul>
            <div class="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </nav>
    </header>

    <!-- Mobile Sidebar -->
    <div class="mobile-sidebar">
        <ul class="nav-menu">
            <li><a href="/">Home</a></li>
            <li><a href="/about">About</a></li>
            <li><a href="/product">Product</a></li>
            <li><a href="/Gallery">Gallery</a></li>
            <li><a href="/Contact" class="active">Contact</a></li>
        </ul>
    </div>

    <!-- Mobile Overlay -->
    <div class="mobile-overlay"></div>

    <main class="main-content">
        <div class="contact-main">
            <h1 class="contact-title">Get In Touch</h1>
            <p class="contact-subtitle">Have questions about our Indonesia tour packages? We'd love to hear from you!</p>
            <div class="contact-card">
			<div class="contact-info">
				<h2>Contact Information</h2>
				<p>Ready to explore the beauty of Indonesia? Contact us today and let us help you plan your perfect adventure!</p>
				<div class="contact-icons">
					<div class="contact-item">
						<span class="icon whatsapp">
							<svg width="28" height="28" viewBox="0 0 32 32" fill="none"><circle cx="16" cy="16" r="16" fill="white" opacity="0.2"/><path d="M22.5 19.5c-.4-.2-2.3-1.1-2.6-1.2-.3-.1-.5-.2-.7.2-.2.4-.8 1.2-1 1.4-.2.2-.4.3-.8.1-.4-.2-1.7-.6-3.2-2-1.2-1.1-2-2.4-2.2-2.8-.2-.4 0-.6.2-.8.2-.2.4-.5.6-.7.2-.2.2-.4.3-.6.1-.2 0-.5-.1-.7-.1-.2-.7-1.7-.9-2.3-.2-.6-.5-.5-.7-.5-.2 0-.5 0-.7 0-.2 0-.6.1-.9.4-.3.3-1.1 1.1-1.1 2.7 0 1.6 1.2 3.1 1.4 3.3.2.2 2.3 3.5 5.6 4.7.8.3 1.4.5 1.9.6.8.1 1.5.1 2.1.1.6 0 1.8-.6 2.1-1.2.3-.6.3-1.1.2-1.2z" fill="#fff"/></svg>
						</span>
						<div class="contact-details">
							<span class="contact-label">WhatsApp</span>
							<span class="contact-value">+62 812-3456-7890</span>
						</div>
					</div>
					<div class="contact-item">
						<span class="icon email">
							<svg width="28" height="28" viewBox="0 0 32 32" fill="none"><circle cx="16" cy="16" r="16" fill="white" opacity="0.2"/><path d="M8 12v8h16v-8l-8 5-8-5z" fill="#fff"/><path d="M8 12l8 5 8-5" stroke="#fff" stroke-width="2"/></svg>
						</span>
						<div class="contact-details">
							<span class="contact-label">Email</span>
							<span class="contact-value">info@exploreindonesia.com</span>
						</div>
					</div>
					<div class="contact-item">
						<span class="icon location">
							<svg width="28" height="28" viewBox="0 0 32 32" fill="none"><circle cx="16" cy="16" r="16" fill="white" opacity="0.2"/><path d="M16 8a6 6 0 0 0-6 6c0 4.5 6 10 6 10s6-5.5 6-10a6 6 0 0 0-6-6zm0 8a2 2 0 1 1 0-4 2 2 0 0 1 0 4z" fill="#fff"/></svg>
						</span>
						<div class="contact-details">
							<span class="contact-label">Office</span>
							<span class="contact-value">Jl. Sudirman No. 123<br>Jakarta, Indonesia</span>
						</div>
					</div>
				</div>
			</div>
			<form class="contact-form">
				<div class="form-row">
					<div class="form-group">
						<label for="name">Your Name *</label>
						<input id="name" type="text" placeholder="Enter your name" required />
					</div>
					<div class="form-group">
						<label for="email">Your Email *</label>
						<input id="email" type="email" placeholder="Enter your email" required />
					</div>
				</div>
				<div class="form-group">
					<label for="subject">Your Subject *</label>
					<input id="subject" type="text" placeholder="Enter subject" required />
				</div>
				<div class="form-group">
					<label for="message">Message</label>
					<textarea id="message" rows="5" placeholder="Type your message..."></textarea>
				</div>
				<button type="submit">SEND MESSAGE</button>
			</form>
		</div>
        </div>
    </main>
<style>
.main-content {
	flex: 1;
	margin-bottom: 60px;
}
.contact-main {
	margin-top: 100px;
	display: flex;
	flex-direction: column;
	align-items: center;
}
.contact-title {
	font-family: 'Cormorant Garamond', serif;
	font-size: 2.5rem;
	text-align: center;
	margin-bottom: 0.5rem;
}
.contact-subtitle {
	font-family: 'Noto Sans', sans-serif;
	text-align: center;
	margin-bottom: 2rem;
}
.contact-card {
	display: flex;
	width: 90%;
	max-width: 1000px;
	background: #fff;
	border-radius: 20px;
	box-shadow: 0 8px 32px rgba(0,0,0,0.1);
	padding: 0;
	gap: 0;
	transition: box-shadow 0.3s, transform 0.3s;
	overflow: hidden;
}
.contact-card:hover {
	box-shadow: 0 8px 32px rgba(0,0,0,0.12);
	transform: translateY(-4px);
}
.contact-info {
	background: linear-gradient(135deg, #a8b98a 0%, #95a671 100%);
	color: #fff;
	border-radius: 20px 0 0 20px;
	padding: 30px 25px;
	width: 40%;
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	position: relative;
}
.contact-info h2 {
	font-family: 'Cormorant Garamond', serif;
	font-size: 2rem;
	margin-bottom: 10px;
}
.contact-info p {
	font-family: 'Noto Sans', sans-serif;
	margin-bottom: 30px;
}
.contact-icons {
	display: flex;
	flex-direction: column;
	gap: 20px;
	margin-top: 20px;
}
.contact-item {
	display: flex;
	align-items: center;
	gap: 15px;
}
.contact-details {
	display: flex;
	flex-direction: column;
}
.contact-label {
	font-size: 0.9rem;
	opacity: 0.8;
	margin-bottom: 2px;
}
.contact-value {
	font-size: 1rem;
	font-weight: 500;
}
.icon {
	font-size: 2rem;
	color: #fff;
	flex-shrink: 0;
}
.contact-form {
	width: 60%;
	padding: 30px 20px;
	display: flex;
	flex-direction: column;
}
.form-row {
	display: flex;
	gap: 20px;
	margin-bottom: 20px;
}
.form-group {
	flex: 1;
	display: flex;
	flex-direction: column;
}
.contact-form label {
	color: #a8b98a;
	font-family: 'Noto Sans', sans-serif;
	margin-bottom: 8px;
	font-weight: 500;
}
.contact-form input,
.contact-form textarea {
	border: none;
	border-bottom: 2px solid #a8b98a;
	background: transparent;
	margin-bottom: 10px;
	font-size: 1rem;
	padding: 12px 0;
	font-family: 'Noto Sans', sans-serif;
	outline: none;
	transition: border-color 0.3s ease;
}
.contact-form input:focus,
.contact-form textarea:focus {
	border-bottom-color: #ff6b6b;
}
.contact-form textarea {
	resize: vertical;
	min-height: 120px;
}
.contact-form button {
	align-self: flex-end;
	border: 2px solid #ff6b6b;
	color: #ff6b6b;
	background: transparent;
	border-radius: 20px;
	padding: 8px 30px;
	font-family: 'Noto Sans', sans-serif;
	font-weight: 500;
	margin-top: 10px;
	cursor: pointer;
	transition: background 0.2s, color 0.2s, box-shadow 0.2s;
	box-shadow: 0 2px 8px rgba(255,107,107,0.08);
}
.contact-form button:hover {
	background: #ff6b6b;
	color: #fff;
	box-shadow: 0 4px 16px rgba(255,107,107,0.18);
}
@media (max-width: 900px) {
	.main-content {
		margin-bottom: 50px;
	}
	.contact-card {
		flex-direction: column;
		border-radius: 20px;
		padding: 0;
		width: 95%;
	}
	.contact-info {
		width: 100%;
		border-radius: 20px 20px 0 0;
		padding: 25px 20px;
	}
	.contact-form {
		width: 100%;
		padding: 25px 20px;
	}
	.form-row {
		flex-direction: column;
		gap: 15px;
	}
	.contact-form button {
		align-self: stretch;
		margin-top: 20px;
		padding: 12px 30px;
	}
	.contact-title {
		font-size: 2.2rem;
	}
	.contact-subtitle {
		font-size: 1rem;
		padding: 0 15px;
	}
	.contact-main {
		margin-top: 80px;
	}
	.contact-info h2 {
		font-size: 1.8rem;
		margin-bottom: 15px;
	}
	.contact-info p {
		font-size: 0.95rem;
		margin-bottom: 25px;
	}
}

@media (max-width: 600px) {
	.main-content {
		margin-bottom: 40px;
	}
	.contact-main {
		margin-top: 70px;
		padding: 0 10px;
	}
	.contact-title {
		font-size: 1.8rem;
		margin-bottom: 0.5rem;
	}
	.contact-subtitle {
		font-size: 0.9rem;
		margin-bottom: 1.5rem;
		padding: 0 10px;
	}
	.contact-card {
		width: 100%;
		border-radius: 15px;
		box-shadow: 0 4px 20px rgba(0,0,0,0.08);
	}
	.contact-info {
		padding: 20px 15px;
		border-radius: 15px 15px 0 0;
	}
	.contact-info h2 {
		font-size: 1.5rem;
		margin-bottom: 10px;
	}
	.contact-info p {
		font-size: 0.9rem;
		margin-bottom: 20px;
		line-height: 1.5;
	}
	.contact-icons {
		gap: 15px;
		margin-top: 15px;
	}
	.contact-item {
		gap: 12px;
	}
	.contact-details {
		flex: 1;
	}
	.contact-label {
		font-size: 0.8rem;
	}
	.contact-value {
		font-size: 0.9rem;
		line-height: 1.4;
	}
	.contact-form {
		padding: 20px 15px;
	}
	.contact-form label {
		font-size: 0.9rem;
		margin-bottom: 6px;
	}
	.contact-form input,
	.contact-form textarea {
		font-size: 0.9rem;
		padding: 10px 0;
		margin-bottom: 15px;
	}
	.contact-form textarea {
		min-height: 100px;
	}
	.contact-form button {
		padding: 12px 25px;
		font-size: 0.9rem;
		border-radius: 15px;
		margin-top: 15px;
	}
}

@media (max-width: 480px) {
	.main-content {
		margin-bottom: 30px;
	}
	.contact-main {
		margin-top: 60px;
		padding: 0 5px;
	}
	.contact-title {
		font-size: 1.6rem;
	}
	.contact-subtitle {
		font-size: 0.85rem;
		padding: 0 5px;
	}
	.contact-card {
		border-radius: 12px;
	}
	.contact-info {
		padding: 18px 12px;
		border-radius: 12px 12px 0 0;
	}
	.contact-info h2 {
		font-size: 1.4rem;
	}
	.contact-info p {
		font-size: 0.85rem;
	}
	.contact-form {
		padding: 18px 12px;
	}
	.contact-form input,
	.contact-form textarea {
		font-size: 0.85rem;
		padding: 8px 0;
	}
	.contact-form button {
		padding: 10px 20px;
		font-size: 0.85rem;
	}
	.icon svg {
		width: 24px;
		height: 24px;
	}
}
}
</style>

<script>
	// Fade-in animation on page load
	document.addEventListener('DOMContentLoaded', function() {
		setTimeout(() => {
			document.body.style.opacity = '1';
		}, 50);
	});
	
	// Fade-out animation on navigation
	document.addEventListener('DOMContentLoaded', function() {
		document.querySelectorAll('.nav-menu a').forEach(item => {
			item.addEventListener('click', function(this: HTMLAnchorElement, e) {
				const href = this.getAttribute('href');
				console.log('Clicked link:', href); // Debug
				if (href && !href.startsWith('#') && !href.startsWith('/contact')) {
					e.preventDefault();
					document.body.classList.add('fade-out');
					setTimeout(() => {
						window.location.href = href;
					}, 500);
				}
			});
		});
	});
	
	// Header scroll effect
	window.addEventListener('scroll', function() {
		const header = document.querySelector('.header');
		const logo = document.querySelector('.logo');
		const navLinks = document.querySelectorAll('.nav-menu a');
		
		if (window.scrollY > 50 && header && logo) {
			header.classList.add('scrolled');
			(header as HTMLElement).style.background = 'rgba(255, 255, 255, 0.95)';
			(header as HTMLElement).style.backdropFilter = 'blur(20px)';
			(header as HTMLElement).style.boxShadow = '0 2px 20px rgba(0, 0, 0, 0.1)';
			(header as HTMLElement).style.borderBottom = '1px solid rgba(255, 255, 255, 0.2)';
			(logo as HTMLElement).style.color = '#333';
			(logo as HTMLElement).style.textShadow = 'none';
			navLinks.forEach(link => {
				(link as HTMLElement).style.color = '#333';
				(link as HTMLElement).style.textShadow = 'none';
			});
		} else if (header && logo) {
			header.classList.remove('scrolled');
			(header as HTMLElement).style.background = 'transparent';
			(header as HTMLElement).style.backdropFilter = 'none';
			(header as HTMLElement).style.boxShadow = 'none';
			(header as HTMLElement).style.borderBottom = 'none';
			(logo as HTMLElement).style.textShadow = 'none';
			navLinks.forEach(link => {
				(link as HTMLElement).style.textShadow = 'none';
			});
		}
	});

// Mobile Menu Toggle
document.addEventListener('DOMContentLoaded', function() {
	const hamburger = document.querySelector('.hamburger');
	const mobileSidebar = document.querySelector('.mobile-sidebar');
	const mobileOverlay = document.querySelector('.mobile-overlay');
	const navLinks = document.querySelectorAll('.mobile-sidebar .nav-menu a');

	if (hamburger && mobileSidebar && mobileOverlay) {
		hamburger.addEventListener('click', function(this: HTMLElement) {
			this.classList.toggle('active');
			mobileSidebar.classList.toggle('active');
			mobileOverlay.classList.toggle('active');
			document.body.style.overflow = mobileSidebar.classList.contains('active') ? 'hidden' : '';
		});

		mobileOverlay.addEventListener('click', function(this: HTMLElement) {
			hamburger.classList.remove('active');
			mobileSidebar.classList.remove('active');
			this.classList.remove('active');
			document.body.style.overflow = '';
		});

		navLinks.forEach(link => {
			link.addEventListener('click', function() {
				hamburger.classList.remove('active');
				mobileSidebar.classList.remove('active');
				mobileOverlay.classList.remove('active');
				document.body.style.overflow = '';
			});
		});

		// Close sidebar when pressing Escape key
		document.addEventListener('keydown', function(e) {
			if (e.key === 'Escape' && mobileSidebar.classList.contains('active')) {
				hamburger.classList.remove('active');
				mobileSidebar.classList.remove('active');
				mobileOverlay.classList.remove('active');
				document.body.style.overflow = '';
			}
		});
	}
});
</script>

<!-- Footer -->
<Footer />
</body>
</html>
